# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Firebase emulator debug logs
firebase-debug.log
firestore-debug.log
ui-debug.log
database-debug.log
auth-debug.log
.firebase/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Android build outputs (regenerated by Gradle every build — never commit these)
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/captures/
android/*.iml
android/.idea/

# Android signing key — NEVER commit this. If it's ever accidentally
# committed and pushed, treat the key as compromised and start over,
# since GitHub history is very hard to truly scrub.
*.keystore
*.jks
key.properties
